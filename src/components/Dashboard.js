import React from 'react';




const Dashboard = ({children}) => {
  // EL DASHBOARD SE RETURILIZA -> PERO LA LOGICA LO LLEVAMOS A OTRO LADO USANDO REACT ROUTER DOM

  return (
    <>
        {
          children
        }
    </>
  );
}

export default Dashboard;
